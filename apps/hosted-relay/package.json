{
  "name": "@callout-relay/hosted-relay",
  "version": "0.5.3",
  "private": true,
  "description": "Multi-tenant hosted relay: one Durable Object per streamer, pure caption fan-out.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "npx --yes wrangler dev",
    "deploy": "npx --yes wrangler deploy"
  }
}
