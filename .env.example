# Callout Relay API keys (local only — gitignored)
DEEPGRAM_API_KEY=
GEMINI_API_KEY=

# Optional: run relay standalone on a port (default 8787)
# RELAY_PORT=8787
# Optional: mock STT/Gemini for dev without hitting APIs (1 = on)
# RELAY_MOCK_STT=1
# RELAY_MOCK_GEMINI=1
