{
  "name": "@callout-relay/companion",
  "version": "0.5.2",
  "private": true,
  "description": "Shared capture + relay client + local control API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "browser": {
    "./dist/index.js": "./dist/browser.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@callout-relay/shared": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
